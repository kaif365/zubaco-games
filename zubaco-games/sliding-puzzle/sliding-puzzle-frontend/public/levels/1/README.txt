Place the first level image pair here using these exact filenames:

- find.png
- reference.png

The app already points level 1 at these paths and falls back to the seeded placeholder image if they are missing.
