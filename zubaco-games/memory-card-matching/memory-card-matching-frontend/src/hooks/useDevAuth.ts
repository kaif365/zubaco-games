/**
 * @deprecated Use `useAuthGate` via `AppBootstrapGate` instead.
 * Kept for existing unit tests that exercise dev-session fetch behavior.
 */
export { useAuthGate as useDevAuth } from '@/hooks/useAuthGate';
